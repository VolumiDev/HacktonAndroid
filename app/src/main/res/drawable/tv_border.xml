<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo del TextView (opcional) -->


    <!-- Definir el borde (color y grosor) -->
    <stroke
        android:width="8dp"
        android:color="#FFFffF" /> <!-- Cambia el color según prefieras -->

    <!-- Esquinas redondeadas (opcional) -->
    <corners android:radius="20dp" />

    <!-- Margen interno (opcional) -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
